{namespace Templates.Dropdown}

/**
 * @param? elementClasses
 * @param id
 */
{template .content}
	<div id="{$id}" class="dropdown component{$elementClasses ? ' ' + $elementClasses : ''}">
		{call .header data="all" /}
		{call .body data="all" /}
	</div>
{/template}

/**
 * @param header
 * @param id
 */
{template .header}
	<div id="{$id}-header">
		{if $header}
			{$header}
		{/if}
	</div>
{/template}

/**
 * @param body
 * @param id
 */
{template .body}
	<ul id="{$id}-body" class="dropdown-menu">
		{if $body}
			{$body}
		{/if}
	</ul>
{/template}
